import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:["images.unsplash.com","lh3.googleusercontent.com","www.codewithrandom.com","cdn.vectorstock.com"]
  }
};

export default nextConfig;
